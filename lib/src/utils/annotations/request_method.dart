/// Create url to method
/// [url] define URL
class RequestMethod {
  final String method;
  final String methodPath;
  final String nameMethod;

  const RequestMethod(this.method, this.methodPath, this.nameMethod);
}
